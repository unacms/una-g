<?php
/**
 * Bootstrap for CI / tests that do not need a full UNA install.
 * The default bootstrap.php requires inc/header.inc.php and dies without it.
 */
if (!defined('BX_DOL'))
    define('BX_DOL', 1);
